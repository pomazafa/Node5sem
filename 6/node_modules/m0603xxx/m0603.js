const sendmail = require('sendmail')();

module.exports = (message) => {
  sendmail({
    from: 'lab6@nodejs.com',
    to: 'andrei.losik@inbox.ru',
    subject: 'Lab 6 test',
    html: message,
  }, function (err, reply) {
    console.log(err && err.stack);
    console.dir(reply);
  });
};
